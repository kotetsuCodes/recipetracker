import React from 'react';

var Firebase = require('firebase');
var ReactFireMixin = require('reactfire');

var ShoppingLists = React.createClass({

  render: function() {
    return (
      <div>
        <h4>Shopping Lists</h4>
      </div>
    )
  }

})

export default ShoppingLists;
